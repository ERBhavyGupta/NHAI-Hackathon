# B2 Backend — Python dependencies (updated for compatibility)
# Install: pip install -r requirements.txt

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Liveness
mediapipe==0.10.35
opencv-python-headless==4.9.0.80   # headless = no GUI deps (for server/Lambda)
numpy>=1.24,<2.0

# AWS
boto3==1.34.100
mangum==0.17.0                     # FastAPI ↔ Lambda adapter

# Dev / test
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                      # TestClient needs this
Pillow>=10.0
