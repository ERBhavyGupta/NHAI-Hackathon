/**
 * app/index.tsx
 * Entry point — renders SplashScreen which handles routing.
 */

import SplashScreen from '../screens/SplashScreen';

export default function Index() {
  return <SplashScreen />;
}
